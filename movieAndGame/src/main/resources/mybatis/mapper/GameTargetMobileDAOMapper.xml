<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.movieAndGame.DAO.GameTargetMobileDAO">
	<insert id="insert" parameterType="com.movieAndGame.DTO.GameTargetMobileDTO">
		insert into mobile_game_target(title, writer, game_name, target_post, write_date) values(#{title},#{writer},#{gameName},#{targetPost},now())
	</insert>
</mapper>